# If deploy fails with "refs/heads/main" not found, see NETLIFY_DEPLOY_FIX.md
[build]
  command = "npm run build"
  publish = "dist"

# Optional: if you use client-side routing (React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

